{
  "tasks": [
    {
      "id": "task-1",
      "description": "Create the product data module in src/, exporting a Product type and the three seed products as a local dataset. No API client exists, so the data is defined in the module itself.",
      "files": [
        "src/products.ts"
      ],
      "depends_on": [],
      "requirements": [
        "PRODUCT-REQ-001"
      ],
      "priority": "required"
    },
    {
      "id": "task-2",
      "description": "Create src/ProductSearch.tsx as a named-export function component: a controlled search input that filters the products by name case-insensitively, renders the matches, and renders the empty state when none match.",
      "files": [
        "src/ProductSearch.tsx"
      ],
      "depends_on": [
        "task-1"
      ],
      "requirements": [
        "PRODUCT-REQ-001",
        "PRODUCT-REQ-002",
        "PRODUCT-REQ-003"
      ],
      "priority": "required"
    },
    {
      "id": "task-3",
      "description": "Render ProductSearch from the existing src/App.tsx, which main.tsx already mounts. Modify App.tsx rather than adding a new screen.",
      "files": [
        "src/App.tsx"
      ],
      "depends_on": [
        "task-2"
      ],
      "requirements": [
        "PRODUCT-REQ-001"
      ],
      "priority": "required"
    },
    {
      "id": "task-4",
      "description": "Add co-located tests at src/ProductSearch.test.tsx using the configured Vitest globals, React Testing Library and user-event, covering initial visibility, case-insensitive narrowing, and the empty state.",
      "files": [
        "src/ProductSearch.test.tsx"
      ],
      "depends_on": [
        "task-2"
      ],
      "requirements": [
        "PRODUCT-REQ-004"
      ],
      "priority": "required"
    }
  ]
}