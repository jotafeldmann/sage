{
  "tasks": [
    {
      "id": "task-1",
      "description": "Create the product data module exporting the Product type and the three seed products.",
      "files": ["src/products.ts"],
      "depends_on": [],
      "requirements": ["PRODUCT-REQ-001"],
      "priority": "required"
    },
    {
      "id": "task-2",
      "description": "Create the ProductSearch component: a case-insensitive search input filtering the product list by name, rendering the matching products, and showing the empty state when nothing matches.",
      "files": ["src/ProductSearch.tsx"],
      "depends_on": ["task-1"],
      "requirements": ["PRODUCT-REQ-001", "PRODUCT-REQ-002", "PRODUCT-REQ-003"],
      "priority": "required"
    },
    {
      "id": "task-3",
      "description": "Render ProductSearch from the existing App component.",
      "files": ["src/App.tsx"],
      "depends_on": ["task-2"],
      "requirements": ["PRODUCT-REQ-001"],
      "priority": "required"
    },
    {
      "id": "task-4",
      "description": "Add tests covering initial visibility of all products, narrowing via search, and the empty state.",
      "files": ["src/ProductSearch.test.tsx"],
      "depends_on": ["task-2"],
      "requirements": ["PRODUCT-REQ-004"],
      "priority": "required"
    }
  ]
}
