import Container from "@mui/material/Container";
import Typography from "@mui/material/Typography";

export function App() {
  return (
    <Container maxWidth="lg">
      <Typography variant="h1" fontSize="2rem" py={2}>
        Car Inventory
      </Typography>
    </Container>
  );
}
